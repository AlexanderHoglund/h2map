{
  "name": "@h2map/units",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Branded scalar unit types shared across the corridor and LCOH engines. Zero dependencies.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  }
}
