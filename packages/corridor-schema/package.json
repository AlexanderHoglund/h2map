{
  "name": "@h2map/corridor-schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Green Corridor scenario/result types, zod validation, reference-bundle accessors, and the override>derived>benchmark resolution layer.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  },
  "dependencies": {
    "@h2map/units": "*",
    "zod": "^4.1.5",
    "@h2map/fuel-emissions": "*"
  }
}
