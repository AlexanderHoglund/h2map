{
  "name": "@h2map/profile-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Resource-profile service: provider clients (Open-Meteo, PVGIS, NASA POWER), fallback chain, TMY builder. Zero runtime dependencies; fetch and cache are injected.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "test": "vitest run"
  }
}
